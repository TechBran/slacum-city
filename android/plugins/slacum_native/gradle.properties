# Kept deliberately small: the AAR build has no resources, no jetifier work and
# no reason for a 4 GB daemon (the engine build next door already asks for one).
android.useAndroidX=true
org.gradle.jvmargs=-Xmx1536m
org.gradle.caching=true
